
void fun(int args,char *argv)
{
	if(auto*register*static)
	{
		printf(1);
	}
}

void avoid(void *k)
{
	int i,j;
	float dx=0.33;

	if(i==1)
		y=f(x);
	else if(2)
		y=f(x)*dx;
	else
	{
		if(j==1)
		{
			fun(1);
		}
		else
		{
			if(i==1)
				y=f(x);
			else if(2)
				y=f(x)*dx;
			else
			{
				if(i==1)
					y=f(x);
				else if(2)
					y=f(x)*dx;
				else
					y=0;
			}
		}
	}
}
